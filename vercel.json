{
  "functions": {
    "api/recognize.js":       { "maxDuration": 30 },
    "api/analyze-fast.js":    { "maxDuration": 20 },
    "api/analyze-refine.js":  { "maxDuration": 60 }
  },
  "rewrites": [
    {
      "source": "/((?!api/)(?!@)(?!.*\\.[^/]+$).*)",
      "destination": "/index.html"
    }
  ]
}
